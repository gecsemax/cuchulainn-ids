```text
                         ┌──────────────────────────────────────────┐
                         │        Traffic Generator Machine         │
                         │------------------------------------------│
                         │  • MoonGen / tcpreplay                   │
                         │  • Safe synthetic protocol traffic       │
                         │  • Labeled benign + suspicious patterns  │
                         │  • 1–10 Gbps output sweep                │
                         └───────────────┬──────────────────────────┘
                                         │
                                         │ 10GbE Direct Link
                                         ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                         CuChulainn IDS Machine                             │
│────────────────────────────────────────────────────────────────────────────│
│  • AF_PACKET raw capture (non‑blocking)                                    │
│  • epoll event loop                                                        │
│  • AVX‑512 accelerated protocol detection                                  │
│  • Per‑protocol parsers (TLS/DNS/HTTP1/HTTP2/SMTP/IMAP/POP3/FTP/SIP/NTP)   │
│  • ML zero‑day detection (optional)                                        │
│  • Zero‑allocation hot path                                                │
│                                                                            │
│  Measurement Points:                                                       │
│    → Throughput (Gbps, pps)                                                │
│    → Packet loss                                                           │
│    → Latency                                                               │
│    → CPU usage                                                             │
│    → Detection rate                                                        │
└────────────────────────────────────────────────────────────────────────────┘
